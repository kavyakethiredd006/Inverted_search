# Inverted_search
An Inverted Search project in C that implements an inverted index to enable fast text searching across multiple files. It maps words to their occurrences in documents, allowing efficient keyword-based retrieval. Uses data structures, file handling, and hashing for quick search and indexing operations.
